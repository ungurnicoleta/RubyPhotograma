<%= form_with(model: photo_tag, local: true) do |form| %>
  <% if photo_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_tag.errors.count, "error") %> prohibited this photo_tag from being saved:</h2>

      <ul>
        <% photo_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :photo_id %>
    <%= form.text_field :photo_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.number_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
